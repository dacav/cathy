- keep stats
 - number of effective hash collisions (not a copy, yet a collision)
 - number of copies with wrong dates

- handle the case of re-scan of output directory
- hierarchy by time, y/m/d
- test (unlikely) file collision by using mocky cmp program
